// import type {
//   SandboxStoreState,
//   Notify
// } from '../store/sandboxStore'

// import type {
//   SandboxTable
// } from '../types/sandbox.types'

// export const notify = (
//   cb?: Notify,
//   msg?: string
// ) => {
//   if (cb && msg) {
//     cb(msg)
//   }
// }

// export const getActiveTable = (
//   state: SandboxStoreState
// ): SandboxTable | undefined =>
//   state.tables.find(
//     t => t.id === state.activeTableId
//   ) || state.tables[0]

